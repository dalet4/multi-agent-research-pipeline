# Multi-Agent Research Pipeline Configuration

# LLM Configuration (choose one or configure both)
LLM_PROVIDER=gemini  # Options: openai, gemini

# OpenAI Configuration
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-4o  # Options: gpt-4o, gpt-4o-mini

# Gemini Configuration (recommended for search processing)
GEMINI_API_KEY=AIza...
GEMINI_MODEL=gemini-1.5-pro  # Options: gemini-1.5-pro, gemini-1.5-flash

# Search APIs (Primary: Tavily, Fallback: SerpAPI)
TAVILY_API_KEY=tvly-...  # Get from https://tavily.com - 1000 searches/month free
SERP_API_KEY=...  # Get from https://serpapi.com - 100 searches/month free

# Gmail OAuth Configuration
GMAIL_CREDENTIALS_PATH=./credentials/credentials.json

# Optional: Search Strategy Configuration
SEARCH_STRATEGY=intelligent  # Options: tavily_only, serp_only, intelligent
MAX_SEARCH_RESULTS=10
SEARCH_TIMEOUT=30

# Logging Configuration
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR
LOG_FILE=./logs/research_agent.log